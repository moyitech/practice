<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carbon.system.mapper.SysRoleMapper">

    <select id="getAccountRole" resultType="com.carbon.system.vo.SysAccountRoleVo">
        select 
            sar.account_id accountId,
            group_concat(sr.id) roleIds,
            group_concat(sr.role_name) roleNames
        from sys_account_role sar
        left join sys_role sr on sr.id = sar.role_id
        where sar.account_id = #{accountId}
        group by sar.account_id
    </select>

</mapper>








